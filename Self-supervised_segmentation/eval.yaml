
program: eval.py
method: grid

name: sweep
project: UNET_OCM
entity : mohamad_hawchar

metric:
  goal: maximize
  name: jaccard

parameters:
  # image_size:
  #   value: 384
  # pretrained_weights:
  #   values: [
  #     "/home/mohamad_h/output/vit_small/VIT_8_MSC_384_8B_0.3R_8MP/ckpt_epoch_10.pth"
  #     ]
  crop:
    values: [1]
  method:
    values: [otsu, k-means, chan-vese, chan-vese_ours, k-means_feature_clustering]
      
