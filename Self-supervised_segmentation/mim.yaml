
program: mim.py
method: grid

name: sweep
project: EARLY_STOPPAGE
entity : mohamad_hawchar

metric:
  goal: maximize
  name: jaccard

parameters:
  image_size:
      value: 320
  mask_patch_size:
    values: [8,16]
  mask_ratio:
    values: [ 0.3, 0.4, 0.6]
  early_stopping:
    values: [True, False]
      
